Future<dynamic> callApi(options){
  // implment a reusable network caller that returns
  return Future.delayed(
    const Duration(microseconds: 500), 
    (){
      return true;
    }
  );
}

